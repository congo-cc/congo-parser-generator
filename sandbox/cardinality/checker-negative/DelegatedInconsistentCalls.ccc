// Expect error: Child invoked both inside a loop and outside it
PARSER_PACKAGE="org.parsers.test.checker";
TEST_PRODUCTION=Root;

Root : Parent ;

Parent : ( Child )+ Child ;

Child : ENSURE {&0:1&} "x" ;
