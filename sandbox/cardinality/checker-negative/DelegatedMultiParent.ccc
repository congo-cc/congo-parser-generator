// Expect success: Child invoked from two distinct iterating expansions (multi-parent bias).
PARSER_PACKAGE="org.parsers.test.checker";
TEST_PRODUCTION=Root;

Root : Parent ;

Parent : ( Child )+ ( Child )* ;

Child : ENSURE {&0:1&} "x" ;
