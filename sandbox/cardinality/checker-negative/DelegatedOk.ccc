// Expect success: orphan RCAs in Child bind to Parent's OneOrMore (Phase 1 linking)
PARSER_PACKAGE="org.parsers.test.checker";
TEST_PRODUCTION=Root;

Root : Parent ;

Parent : ( Child )+ ;

Child : ENSURE {&0:1&} "x" ;
